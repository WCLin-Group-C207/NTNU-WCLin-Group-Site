# 低風險項目白名單。命中即放行，不會被 commit hook 擋下。
# 一行一筆，可用 # 開頭整行註解。三種寫法：
#
# 1. 完整 email，逐字比對（大小寫不分）：
#      collab@partner-lab.org
#
# 2. 網域，比對方式跟 academic_domains.txt 一樣採「結尾比對」：
#      partner-lab.org
#    （會放行任何以這個字串結尾的網域，包含子網域）
#
# 3. 完整台灣手機號碼（純數字 10 碼），逐字比對：
#      0912345678
#
# 新增項目後直接重新執行 git commit 即可，不需要改程式碼。
